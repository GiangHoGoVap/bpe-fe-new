import { createStyles, rem } from '@mantine/core';

export const useCreateWorkspaceButtonStyle = createStyles(() => ({
    buttonIcon: {
        width: rem(15),
        height: rem(15),
    },
    input: {
        width: '100%',
    }
}));
